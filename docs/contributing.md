---
title: Contributing
---

<!--
This page is `CONTRIBUTING.md` from the repository root, pulled in by pymdownx.snippets rather
than copied. GitHub looks for that file at the root when somebody opens a pull request and
mkdocs needs a page under `docs/`, so one file holds the prose and this one includes it. The
include is configured in `mkdocs.yml`, with `check_paths: true` so a path that stops resolving
fails the build instead of publishing the literal directive.

Relative links inside the included file resolve against THIS page, not the repository root -
see the note in CONTRIBUTING.md itself.
-->

--8<-- "CONTRIBUTING.md"
