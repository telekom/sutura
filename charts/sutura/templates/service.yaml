apiVersion: v1
kind: Service
metadata:
  name: {{ include "sutura.fullname" . }}
  labels:
    {{- include "sutura.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sutura.selectorLabels" . | nindent 4 }}
