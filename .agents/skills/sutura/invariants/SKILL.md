---
name: invariants
description: The guarantees held by a type, a lint or a gate - and, for each, the limit it does not cover. The limit column is the point: it is what no grep of the code will tell you. Open before changing a mechanism or claiming anything is enforced.
---

# Invariants

Held by a **type, a lint, a hook or a gate - never by recall.** Changing one is an architecture
decision. **A row that loses its mechanism gets deleted, not demoted to advice**; a rule that never
had one belongs in `secure-by-design` instead.

`cargo xtask --help`, `clippy.toml` and the workspace lint table tell you *what* runs. This file
exists for the other column: **what each control does not reach.** An overstated control spends
trust a reviewer needed elsewhere, so an overstated claim is itself the defect.

House pattern: a `compile_fail` doctest always comes with a compiling twin differing by exactly the
line under test, verified non-vacuous by unmarking it. A `compile_fail` with no twin proves the
snippet is broken, not that the rule holds.

## The surface

| Guarantee | Held by | Does not reach |
| --- | --- | --- |
| No SQL, table, predicate or row-id on the tool surface | `Query` declares no such field; `deny_unknown_fields` names the attempt | - |
| A refusal is a value, not an `Err` | `ToolOutcome::Refusal`, and two per-transport exhaustive matches with no wildcard arm, so a new variant fails to compile in both | Nothing compares the two *sentences*, and nothing should - different readers |
| Every `RefusalReason` variant is provoked, or excused with a date | `check-refusal-coverage` | **A file naming EVERY variant is a census and is evidence for none** - five exist. And a NAME is not a provocation: a variant mentioned in a test that does not trigger it counts, because deciding otherwise needs to know what a test asserts |
| A caller cannot state its own identity | Nothing in `identity::principal` implements `Deserialize`; the verified path needs a `VerifiedCaller` whose one constructor is `pub(crate)` | The reviewer's question is "can a request PRODUCE this type", not "can it reach this parameter" |
| A granted scope gates the operation | `Capability`'s three exhaustive matches; `RouteNotGoverned` refuses a router with an ungoverned route | **Never which rows an answer contains.** And nothing narrows the agent surface: a pipe has no header a token could arrive in, so `serve_stdio` passes every capability |
| Every outcome is recorded before it is returned | `AuditSink` returns nothing a caller can branch on; `LocalService::start` requires a sink and `Surface::answer` writes the record before its `Ok`; a sixth half of `check-boundaries` refuses a **caller of the driving port** that names `sutura_app::answer` in its own `src/` | **Recording is not a control** - it reaches a caller after the rows did, and sutura retains nothing. A `SurfaceFailure` is not an outcome, so an `Err` writes no record. Six more limits below this table |
| At most `runtime.max_concurrent_queries` questions are executing, on either transport | Both transports' constructors REQUIRE the bound and neither may build one: `Admission::new` is `pub(crate)`, so the only public door is `from_settings` - the group the two keys live in - and `sutura_http::ServiceState::new` and `sutura_mcp::AgentSurface::new` both TAKE the value. The permit is a value the blocking closure OWNS, so a peer that stops waiting cannot hand it back before the work ends | **It cancels nothing** - `Warehouse` is synchronous and carries no deadline, so a question inside the pool runs to completion and holds its slot until it does (`telekom/sutura#160`). Not per-caller: one peer can fill every slot. On the agent surface the *shed* answer is exercised over the wire and the permit-with-the-work half is asserted by dropping the handler future in a test, because rmcp 3.1.4 drops it on neither cancellation nor disconnect |
| One PROCESS builds one `Admission` | `check-one-bound`, plus the type half above. Five rules: a construction only in a crate with a `src/main.rs`, at most one per crate, one in every such crate that composes a transport, every transport-composition needle still CALLED, and the door still defined where it reads it. Roots are DERIVED from `src/main.rs`, so a third one is covered the day it is written. `sutura_http::ServiceState::new` derived its own until `telekom/sutura#340`, which made a second state a second permit set - and the fact is asserted, not just the count: `one_bound_handed_to_two_states_is_one_permit_set` fills the bound through one state and watches the other shed, then answers through it after the release so the `503` cannot be a broken router | **It counts CONSTRUCTIONS, and it counted LINES for one review round** - `let (a, b) = (Admission::from_settings(r), Admission::from_settings(r));` fits `max_width = 130`, so rustfmt keeps it as one line and the gate printed `ok - 2` over a tree that built three, with `fmt`, `lint` and `hygiene` all green. One call in a loop is still one construction and many bounds. A bound reached through a function pointer, a trait method, a re-export or `<Admission as Trait>::from_settings` is invisible, and so is a second `[[bin]]` in one crate (none exists - a crate is one process here because a crate has one binary here). `crates/` only. **No process serves both transports**, so *two transports, one bound* is asserted over two `ServiceState`s and not over an HTTP surface beside an agent one - the two transport crates may not reach each other and each root links one. The rename refusal covers `use ... as` **and** `type X = Admission` (the second was open, measured green over a tree that built three, and survives at module scope only - an in-body `type` is refused by `clippy::items_after_statements`); it cannot follow a `type` split across two lines |
| A caller's WHOLE wait is bounded by `server.request_timeout_seconds`, on either transport | HTTP: an outer `tower` layer answers `408` with the documented body, so the admission wait is inside it. Agent surface: `AgentSurface::new` REQUIRES the key and one deadline wraps both waits - `telekom/sutura#339`, where the admission window was the only bounded wait and a question that GOT a slot waited as long as the data system took. Two cells at two configured numbers, so a constant fails one of them, plus a window LONGER than the deadline proving the deadline covers the wait for a slot | **It bounds the WAIT, not the question**: the permit is the closure's, so a timed-out question keeps its slot until the data system answers - asserted, with the slot still gone and the worker still in the port. **The two transports meant two different things by this key for one review round**: the deadline was applied AFTER `admit` here, so a peer's worst case was `admission_timeout_seconds + request_timeout_seconds`, found by reading both paths rather than by a test. Nothing narrows it per caller. And a peer that sends `notifications/cancelled` still stops nothing and learns nothing until the deadline fires: rmcp 3.1.4 delivers the cancellation as `RequestContext::ct` and `call_tool` does not read it (`telekom/sutura#362`) |

### What the audit row does not reach

**The constructor was the whole mechanism, and `sutura query` was outside it** - it called the answer
function directly and wrote nothing, on the shipped binary, while the row named that constructor.
That is why the second mechanism exists, and each of these is a limit on one of the two.

| Limit | Why it is there |
| --- | --- |
| `answer` stays `pub`, so what holds the second half is a TEXT SCAN over callers' `src/` rather than the compiler | The golden suites assert `ServiceError`'s VARIANTS from separate crates, and the driving port erases those into `SurfaceFailure` by design - narrowing the door would take the typed-error assertions out of the conformance suite |
| A crate that is not a caller of the driving port is outside the scan, and so is `#[cfg(test)]` inside one | The rule is about what a published binary does, and test code is not in one |
| The scan reads a PATH rooted at `sutura_app`, and **two of the three ways past that are closed** | Renaming the crate (`use sutura_app as app;`, `use sutura_app::{self as app};`) is REFUSED rather than chased, the way `check-boot-order` refuses a rename of its own tracked name; `use sutura_app::*` is refused by `clippy::wildcard_imports` under `-D warnings`. What is left: a re-export of `answer` through a third crate, and a dependency renamed in a manifest - neither in this tree, and no scan of `src/` could see either |
| **A record is written and not kept** | Neither CLI command installs a tracing subscriber, so `query` and `mcp` both write onto a dispatcher that discards. Only `sutura-serve` installs one |
| **A catalog read is not an outcome**, so `DescribeCatalog` writes no record on any transport | Argued at `sutura_mcp::server::describe`: a listing of what a deployment measures is business information with no row of data in it. Recording it needs a third `RecordedOutcome` variant and a request context on `Surface::definitions` - an architecture decision |
| **`sutura compile` prints a refusal with no record**, and the row does not reach it | It builds no `ToolOutcome`, mints no credential and opens no data system - it renders a plan. Named here rather than left to be found |

**Measured:** with the function renamed and a bypass written to the new name, the gate's own liveness
check - which counted paths rooted at the CRATE - printed `ok` over 94 paths and exited zero. It now
requires the door to still be defined where it reads it, and the green line cites that location.


## The plan and the statement

| Guarantee | Held by | Does not reach |
| --- | --- | --- |
| No value reaches the statement as text | One bind placeholder per plan value, counted per dialect and compared against the plan's parameter list | A value that word-boundedly EQUALS a name the statement quotes is invisible to both negative searches (`month` is a column *and* a legal filter value). Only the placeholder count reaches it |
| Every statement parses under its target dialect | The golden suite parses each statement in the dialect it was generated for | **Narrower than "the data system accepts it".** ClickHouse and BigQuery are never executed. And MEASURED: within one target the parser cannot see a function's ARGUMENT ORDER, so both `DATE_TRUNC` spellings parse as BigQuery - which is why `DateTruncShape` is an exhaustive declaration rather than a check |
| A dialect declares what it folds and how deep it resolves | `identifier_case` and `qualification` are exhaustive matches, so a fifth dialect cannot compile without answering | **A self-check, not a barrier:** a bundle is dialect-agnostic, so both comparisons use `IdentifierCase::COARSEST` whatever a dialect declares. Postgres and ClickHouse are declared from docs, not measured - neither has a server here to ask |
| Two tables one statement reads can be told apart | `StatementTables` is the only way to a `QueryPlan` or a `LegPlan::Fact`, so the ambiguous plan is unrepresentable rather than checked at render time | A query-time refusal, deliberately - a physical table name is not something an author can rename. A `Lookup` leg declares no joins, so what holds there is the shape |
| A join cannot silently change a measure | `Definitions::assemble` refuses a dimension reached through a relationship whose *declared* cardinality may duplicate rows | **Catalog cardinality is a trusted precondition**: nothing checks the declaration against the data, and an anchor cannot see it - an anchor is asked with no dimensions |
| A question cannot span more sources than the answer can combine | Three or more is `PlanSpansTooManySources`; exactly two is split, or `FederationNotExecutable` | The shipped binary refuses **every** two-source question - both its adapters declare `EXECUTES_LEGS = false` |
| Cross-project is one source | A source is *a credential plus a billing project*, not a project; the plan stage collects `SourceName` and nothing from a table path | That a data system PERFORMS such a join is shown by nothing - the acceptance credential's IAM refuses `datasets.create` |
| We never translate SQL | The dialect layer's `transpile` feature is not compiled, so the call does not build | Deliberately **not** a lint: an unresolvable `disallowed-methods` path is silently ignored at the pinned clippy version - verified - so the entry would read as enforcement and do nothing. A later clippy warns instead, so this expires |

## The bundle

| Guarantee | Held by | Does not reach |
| --- | --- | --- |
| A catalog edit cannot change what executes | `PinnedDefinitions::pin` computes the digest from what it stores - no digest parameter, no hasher parameter - and it travels with the answer | - |
| An unvalidated bundle is never served | `verify_and_validate` is `Validated`'s only constructor, in a private module. Boot reaches a data system through `verify_anchor`, which takes **no credential** | An anchor certifies whatever identity the adapter was configured with. What keeps `verify_anchor` to boot is a `clippy.toml` ban plus one `#[expect]`, **not** its input type: `AnchorPlan::of` is a self-check, and a reviewer fabricated a tuple that passed four guards. **Do not cite the type as a control** |
| An anchor with no identity to re-run it under does not boot | `anchors_run_as` returns a three-variant enum whose third is `NoneDeclared` rather than an `Option`, so a reader names the case instead of deciding what an absence permits | It reads a DECLARATION and nothing passes it to the port - it refuses a deployment that could not have verified honestly rather than proving which identity ran |
| A bundle naming an absent table does not boot | `Warehouse::preflight`, one call per dataset, decided once in `sutura_app::preflight::ask` for both serving roots. **Its ORDER** - after the credential is read, before the transport opens - is held by `check-boot-order` in `just hygiene`, reading three call sites per root as text and cross-checking the roots it declares against every file under `crates/` that calls the pre-flight: both roots held it in prose, one of them calling it *a convention this line keeps* | **It reads the bundle each root loaded FIRST**, and both roots load twice - so a model added to the catalog directory between the two loads is caught on a `files` source by `refuse_unattached` and by nothing on a `bigquery` one. A dataset that could not be ASKED is split by `preflight_was_refused`: `401`/`403` refuse the boot naming the grant, everything else - unreachable, undecodable, `404` - is a `WARN` and the deployment serves, the alternative being a skip flag set in the deployment that most needs it. **A live dataset has answered one** since the `bigquery-acceptance` job went green on 2026-09-02, and since 2026-09-04 a live listing has also FAILED and a live listing has reached the DECISION - `ask` producing `Verdict::Absent` off a real `tables.list`, and a missing dataset answered `404` rather than an empty `200`, which is what stops the `#[serde(default)]` decode reading as *every table absent*. **The split itself is still fake-only in the refusing direction**: `401`/`403` needs an identity holding no `bigquery.tables.list`, which the acceptance environment's is not. And what no run reaches is each root's own WORDS - `refuse_absent_tables` is `pub(crate)` in a crate that depends on the adapter The order gate reads TEXT order in one file, so a pre-flight moved into a helper that runs after the listener binds reads the same to it - and the agent root's transport anchor IS inside such a helper, so what is compared there is where that helper is *defined*. A root that omits the pre-flight ENTIRELY is invisible to it - the scan keys on the pre-flight's own name, so it finds the roots that call it and cannot find one that never did. **Measured:** with its root list emptied it printed `ok - 0 composition root(s)` and exited zero, which is why the list is compared against a scan of `crates/` rather than trusted |
| A metadata adapter declares what it cannot supply | `capabilities` is a required associated item with no default | **Nothing refuses a LOAD whose content disagrees with the declaration.** It is a property of the code, not the bundle, so it is not under the digest. Fidelity is a test, not this row |
| Two adapters reading one content produce one `Definitions` | For a metric's dimensions: `Metric::new` takes a `Vec<Dimension>` while the field is a `BTreeMap`, so collapsing a duplicated or case-folded pair before the domain sees it is a compile error rather than a rule two adapters keep in step. One scan, two `InconsistentDefinitions` variants, so an adapter maps one type from both seams. **What a folded pair costs is measured** - `DuckDB` 1.5.5, whose dialect declares `IdentifierCase::InsensitiveAscii`: `SELECT "Region"` over a subquery projecting both `region` and `Region` returns the `region` column's value and raises nothing | **That one parameter only.** The general claim is a test: the golden suite could not see the divergence this replaced, because no fixture declares a duplicate, and the two shipped adapters had answered differently for as long as the constructor took a map - the `DataHub` one contradicted by its own doc comment. The scan is quadratic and nothing caps how many dimensions a metric may declare |
| Catalog knowledge is descriptive only | The prompt is its only consumer; `Query` has no field a phrase fits in; `load()` takes no `RequestContext` | A second consumer is an architecture decision and **nothing mechanical stops one** - flag it in the handoff |
| An operator's `catalog_prose: omitted` reaches every surface that carries the prose | Per carrier, at construction. `sutura_mcp::wire::CatalogContent::of` and `sutura_http::wire::CatalogBody::of` both demand the setting - a `From` reachable without it fails OPEN, which is how the defect arrived twice - and on the MCP reply the `description` field's TYPE is `wire::prose::Carried`, whose only constructor takes the setting, so a description filled without it does not compile. Every reader of the setting matches exhaustively, so a third `CatalogProse` spelling is a compile error where the prose is carried rather than an `else` arm that withholds it | **Nothing counts the carriers.** *No further carrier exists* is a reading of the code, and both defects were a carrier nobody had counted - the second was the other HALF of a reply this record already called covered, found by a test that read `content.first()` and never `structured_content`. The MCP structured shape is pinned by nothing: the byte-compared snapshots are the two tool INPUT schemas and there is no output-schema check. **Measured:** with the setting arriving as an argument rather than read, reintroducing the constant in `sutura-cli`'s `serve` left all 1811 tests green. It narrows what an agent is TOLD, never what it may ASK, and stops no prose that persuades without escaping |
| Note prose is bounded and normalised | `NoteBody::parse` caps bytes and lines; `Phrase::parse` strips invisibles, collapses whitespace, folds case for identity only | **Not a Unicode-normalisation claim** - there is no NFC/NFD anywhere, so a decomposed spelling and a Cyrillic homoglyph are each a second phrase |
| Every authored scalar the catalog carries is parsed | `DimensionValue`, `AnchorValue` and `Description` are newtypes over a private field with `#[serde(try_from)]`, and one function - `authored_scalar` - is the character rule both value types call, so the two cannot drift apart. `check-serde-parse` holds the derive route | **Nothing decides WHICH field wants a newtype.** An `Anchor`'s value was an unparsed `String` behind a `const` constructor and passed that gate the whole time, because a type with no fallible constructor has nothing to bypass - the class it fell in was a description, not a defence. A fifth authored `String` is caught by review. And the markdown adapter parses its anchor value AFTER the decode rather than inside it, because `#[serde(untagged)]` reports that every variant failed and cannot report why - so that one field names the metric instead of a line and column, and the gate cannot see the route at all |

## Identity

Detail and the built-not-wired inventory: `../identity/SKILL.md`.

| Guarantee | Held by | Does not reach |
| --- | --- | --- |
| A source is served under a declared identity or does not boot | `SourcePosture` has no `Default`; its shared variant needs a witness with no `Deserialize`, so a file cannot produce it | The constructors are `pub` - what is closed is the path from a *file*, not from another crate |
| A posture the linked adapter cannot deliver does not boot | `IMPERSONATION` is a required associated constant; `deliverable_by` is called per source per adapter in the composition root, because it is a property of the BUILD | An associated const makes the port not object-safe, so a heterogeneous adapter set wants a closed enum rather than `dyn` |
| No answer leg reaches a data system without a minted credential | `execute` takes a `&Presented` with **no default**; `agrees_with` compares each leg against the adapter's own declared posture | The witness is prose, so equality is the only comparison - a byte-identical forgery is indistinguishable. `Expiry` is read by nothing: the domain has no clock. The BOOT path is exempt by lint, not by type |
| A subject with no credential is refused, not answered as the process | `CredentialUnavailable`, `403` | **Unreachable end to end on the shipped binary**, because the only configuration the shipped broker refuses is one the root will not boot. A broker that could not be *reached* is `503 identity_unavailable` instead - same status as a dead data system, different code |
| An answer says which identity produced each leg | `ExecutedAs` read off `Warehouse::posture`, never the settings tree | Every shipped answer has exactly one entry - no shipped adapter runs a leg |
| An established identity comes from a signature, never a header | `InboundIdentity` is closed with no default; its gateway variant has **no field for a header holding a username**, and a test presents one and gets nobody | Keys come from a file - no JWKS endpoint. Scopes decide operations, not rows |
| Revocation is bounded by a window a caller cannot influence | Age-triggered re-read plus an unknown-`kid` refetch, both compared and stamped in **one** write-lock acquisition | **The caller-driven trigger provably cannot bound revocation** - a revoked key's `kid` is one the cache holds, so nothing fires. That is why both exist. The only shipped source is a local file |
| A gateway assertion's replay window is this deployment's number | `ProofLifetime` caps `exp - iat`; `iat` is required and a forward-dated one is refused | **Nothing binds an assertion to a request and nothing records what has been seen**, so inside the window an intercepted assertion replays - a regression test asserts the replay rather than pretending otherwise |
| A credential cannot be logged or compared by accident | `Secret` has no `Display` and no `PartialEq`, inherited from `secrecy` rather than merely omitted, so a `#[derive]` cannot produce either | `{secret:?}` still compiles, safe only because that formatter cannot reach the value. `expose_secret` is a lint, so `#[allow]` walks past it and doctests are outside it |

## Resources

| Guarantee | Held by | Does not reach |
| --- | --- | --- |
| Too much data is refused, not truncated, and not reported as an outage | `row_limit()` is `max_rows + 1`, so a result AT the cap is distinguishable from one cut off by it. One variant, one code, `413` never `503`, for both bounds. Pinned across the corpus rather than in one snapshot: 115 SQL goldens read `LIMIT 10001` under `crates/*/tests/snapshots`, and `check-guidance` counts them and fails this row when the number drifts - it caught `39` at a real 63 | `Volume` carries no number and cannot - the endpoint reports neither its cap nor the reply's size, and a test asserts the sentence contains no digit. Only the unpublished adapter can reach that bound |
| Operator memory is bounded; exhaustion refuses rather than kills the process | A `GreedyMemoryPool` neither `SessionContext` constructor lets a caller omit | **Not small:** the pool counts operator reservations only - not driver buffering, not `collect()`, not the row set built during conversion. On macOS no boot check is made |
| Nothing spills the asking subject's rows to disk | `DiskManagerMode::Disabled` on every session | - |
| No result cache | There is none to key. Adding one is an architecture decision, keyed on subject first or not at all | - |

## Boundaries

The lint-shaped guarantees - no first-party `unsafe`, no panic path from input, no dead code behind
`pub`, no file over 1000 lines, no unused dependency, `#[expect]` over `#[allow]` - are in the lint
table and `cargo xtask --help`. What is not discoverable there:

| Guarantee | Held by | Does not reach |
| --- | --- | --- |
| The domain acquires no framework dependency | `check-boundaries` walks the whole **transitive** tree against an allowlist, so a framework arriving through an innocuous crate fails too | - |
| The SQL generator is not in the compiler's closure | `FORBIDDEN_EDGES` forbids the edge to the renderer **as well as** to the dialect layer - the second is what stops the first returning transitively | - |
| A driving port is not owned by one of its callers | The caller set is DERIVED (a member declaring a normal dependency on `sutura-app`), so a fifth transport is covered the day it is written. Zero callers is a failure, not a pass | An **allowlist**, not an attempt to tell a driving port from a driven one - which of those a trait IS depends on who implements it, and a text scan may not pretend to answer that. `src/` only; line-oriented |
| An adapter never reaches an adapter of its own KIND | An edge inside one class only - a renderer, process globals and a composition root are legitimate cross-adapter edges. Transitive, so laundering through a third crate is caught. Dev-dependencies are exempt: that is how a corpus reaches a real system | A crate joins a class by NAME, so an adapter called something else is outside every class. A class with ONE member cannot be violated. A class matching NOTHING is a failure, not a pass |
| THREE implementors of the execution port are held to the SAME test bodies for `execute` and `dry_run` | `sutura-conformance`'s packs are generic in the port and name no adapter, and `check-boundaries`' harness half is what holds that: its NORMAL closure may reach `sutura-domain` and no other first-party crate, allowlisted in `ALLOWED_IN_DOMAIN`'s shape and failing closed when it stops reaching the interior. `execute_packs!` emits one `#[test]` per behaviour inside `mod <adapter>`, so a failure names the behaviour. Which leg cell an adapter gets is read from `EXECUTES_LEGS`: the binding states the declaration, a `const` assertion tears a binding that disagrees with the adapter's own constant, and the two directions arrive under different test names. The `#[test]`s and the list `census` compares are ONE macro repetition, so a test cannot be deleted without deleting its census element - and `Behaviour::EVERY` was a THIRD list tied to neither until #348: deleting a variant from it AND from the macro list was measured green in all three bindings at `18 tests run: 18 passed`, every census reporting five behaviours, with `-D warnings` silent because `tests/bound.rs` keeps the pack fn alive. `Behaviour::index` is an exhaustive match now and a `const` block requires `EVERY[i].index() == i`, so that mutation is `E0080` at build time and a NEW variant does not compile without a position; `census` also refuses an empty corpus, and `.config/nextest.toml`'s second override is what keeps its printed ratio and every `DECLINED` on a green run rather than captured. **A networked adapter is bound because `open` returns `Fixture<W>`** (#348): an absence is a value rather than a panic in a fixture, prints `NOT RUN` under the behaviour's own name, and suppresses the census's coverage line entirely - it is deliberately NOT an `Outcome::Declined`, which is about the adapter rather than the venue. Which venue may skip stays `sutura_dev::requirement`'s single decision, so an absent tier a provisioner REQUIRED fails inside `provisioned::here`; and a DECLARED absence is refused by `refuse_a_declared_absence` wherever `SUTURA_DEV_REQUIRE_TIER` is set, in both endings that can see one (written in `not_here` alone it was 6 of 7, the census being the cell that passed) | **It reaches two of the port's methods.** `verify_anchor`, `working_set_exhausted`, `result_did_not_fit`, `preflight` and `preflight_was_refused` are never called, and three of those five carry rows of their own in this file - so a reader who takes *held to the same test bodies* as a statement about `Warehouse` over-reads it in the direction of the resource and boot guarantees. The corpus is two questions over one table and holds none of the three cases `docs/adr/0012` names. **The `dry_run` declination is observed at RUN TIME**, because the port carries no `dry_run` capability constant the way it carries `EXECUTES_LEGS` - weaker than the leg row above, and not the `Warehouse::preflight` row 45 lines up, which is a different method with a different mechanism. Nothing about impersonation, by that record's decision, and nothing about rendered SQL beyond the endpoint accepting it. **And on a machine that provisioned NOTHING, a fixture's declared absence is indistinguishable from a discovered one** - the packs may not depend on the crate that performs discovery (that is the harness rule above, and its own remedy assigns reaching a tier to the adapter's fixture), so what closes it is a venue's own `SUTURA_DEV_REQUIRE_TIER` and there is no such venue there. Measured before that refusal existed: tier UP, requirement set, a fixture answering absent unconditionally - 21 passed, the only tell seven printed lines |
| Every REGISTERED data system is bound to those packs, or is declared unbound with its reason | `check-conformance-bindings`, and the registry side is DERIVED: the golden matrix's `data_systems` arm read out of the tree, with each entry's crate taken from the first path segment of the adapter type it names. Both other candidate sources are wrong and the reasons generalise - `Warehouse` implementors are 27 here and mostly FAKES that no text scan can tell from an adapter, and crate membership counts one nobody registered (`sutura-exec-bigquery`). It holds the reverse direction too (a binding the registry does not name is coverage no emitted matrix schedules) and the two properties the nextest selectors rest on that nothing enforced: the binding's FILE (`tests/conformance.rs`, for `binary(conformance)`) and its wrapper MODULE (for `test(conformance::<name>)`), with the selector COMPUTED from where the invocation sits and printed on green. **What the needle is worth is decided by four refusals**, each measured leaving `duckdb` reported `bound` with its selector printed over a target emitting ZERO cells, with `just hygiene` `ok` and `just lint` exit 0: a `cfg` that is not `cfg(test)`, a one-line string literal spelling the invocation, the invocation inside an uninvoked `macro_rules!` body, and a manifest that turns off test autodiscovery. **Two conservation laws, one per level**, because a count in a message is not one: `Reconciled::of` refuses a decision list that is not one per entry FOUND, and `Classified::of` refuses a split that does not account for every BINDING found - the second added after a duplicate binding was measured overwriting the first, silently. **The exemption list is EMPTY** since #348: it shipped with `postgres` declared unbound because the packs had no way to say *no tier is up here* that was not a pass, and deleting that entry is what binding the adapter did - an exemption excusing something BOUND is itself a failure, so it could not have been left. An exemption naming an unregistered entry, the wrong crate, or something bound fails the gate, reported in the same run as the violations, and `inert` takes the list as a PARAMETER now because all three of those cells read `UNBOUND.first()` and would have become panics when the list emptied. Adding one back is an architecture decision and `no_registered_data_system_is_declared_unbound` is where it is argued | **That a binding COVERS anything**, and **that the test is EMITTED.** A pack whose body returned `Ok` unconditionally passes every census and this gate. The evidence is a written INVOCATION and its POSITION, never an emitted test name: the four refusals above close the shapes that were measured failing open, and the strong form needs a run's machine-readable output, which `docs/adr/0012` records as deferred. `.config/nextest.toml` spells `binary(conformance) + binary(bound)` and nextest exits 96 when a name matches nothing, so the binary's EXISTENCE was already held there rather than here. It reads the `data_systems` arm alone, so a catalog adapter is under no obligation it can see - compile packs do not exist. What is invisible is the STRAY direction of `crates/`: a registered adapter whose type resolves outside it fails CLOSED with the derivation printed, a *binding* written outside it is compared to nothing. A wrapper macro is two shapes - defined in another file and invoked here it fails closed and names the adapter; defined INSIDE the binding file it satisfied the needle until `Place::template`, and what caught that shape before was `unused_macros` under `-D warnings`, not this gate. And the unreadable-file guard fires FIRST, so a *tracked but deleted* binding is reported as `could not read` rather than by adapter name - measured; deleting it from the index names the adapter |
| No first-party `Deref` and no `Borrow` | `Deref` re-exports the inner API so the invariant leaks with it; `Borrow` is *"unofficially unsafe"* - it promises identical hash, compare and order, and the compiler checks nothing, so a case-folding `parse` turns a map lookup into a silent miss. **The gate started green**, so its job is keeping it that way | Comments and string interiors are blanked first, and that is load-bearing: three doc comments here say *there is deliberately no `Deref`*. Matches the last path segment, so a first-party trait named `Deref` is reported too - the safe direction |
| A validated newtype's `Deserialize` goes through its constructor | `check-serde-parse`. **The one that bites**: a derived `Deserialize` writes straight into the private field, on the path carrying untrusted input | Recognised by `-> Result<Self`, so another spelling is invisible - under-claims rather than failing correct code. `impl` blocks matched per FILE. Deliberately ignores a type with no fallible constructor: a derive bypasses nothing there, and a gate that failed them is one somebody disables |
| A type that parses on the way in serializes the way it came | The second half of the same gate. A shipped bug, gated after the fact: `Date` read ISO text and wrote `{year, month, day}`, and the digest is taken over the serialized form | The comparison is over field NAMES. Two structs with the same names serializing differently pass, because deciding otherwise needs serde's own resolution |
| A provisioned service that CAN be hermetic is run by `just validate`, and one that cannot says why | Four links, each a gate: every discoverable service's compose block declares one CI venue; a `nix native` declaration names a `nix/<service>-tier.nix` that must exist; that module must be imported by `flake.nix` **and** its `sutura-<service>-tier` script named inside the `checks = {` block, so a tier only `just` runs does not count; and every check `flake.nix` declares must appear in the `just ci` loop, which is what `just validate` runs. A `compose only` declaration needs a reason and a convergence path, and fails if the tier file exists | **Nothing can decide whether a service COULD be hermetic** - that judgement is the `# Because:` line, which is prose held by review, and it is the whole content of the DataHub row. The chain holds that the declaration and the tree agree, not that the declaration is true. It also cannot see a tier a check builds and never STARTS beyond the script's name appearing, and `just ci` is checked by name, so a check renamed in both places moves silently |
| A gate cannot be hung by a SECOND, unbounded subprocess wait in the compose tier | `check-bounded-wait`. One function waits on a container-runtime child carrying a deadline, and captures to FILES because a pipe puts the hang back; this gate holds the half that function cannot state about itself. Over `xtask/src/compose.rs` and the Rust under `xtask/src/compose/`, read with comments and multi-line string interiors blanked, only ONE file may hold anything that can be blocked by a child: the docker module, **discovered rather than named**, so the waiter may move inside it - plus declared allowances, each excusing ONE needle in ONE file and carrying why that is not an unbounded wait (`lock.rs`'s `lsof` is the only one). **7 needles the wait gate keys on, and the two a list of method names misses are the shape:** five waits plus `Stdio::piped()` and `io::pipe()`, the two *preconditions* for a drain that never reaches EOF - a pipe's creation, never its handoff, since `Stdio::from` is also how the waiter hands over the capture FILES. Gating the READERS instead is incomplete and, here, wrong - `std::fs::read_to_string` is on 8 lines of this tier and reads a file. `disallowed-methods` cannot express any of it: an entry is workspace-wide and `xtask` waits on `git`, `cargo` and `nix` unbounded on purpose. **Started green.** Fails closed five ways: an empty scan, an in-scope file it cannot read, no waiter, two waiters, and an allowance whose needle no longer matches | **LOCALITY, not boundedness** - it says nothing that can block on a child was written here, not that every child is bounded. Per FILE, so one added beside the bounded loop INSIDE the waiter is invisible, and that file today holds two documented unbounded calls next to it. **The RECEIVER is not knowable from a line**, so `.output()`, `.status()` and `.wait()` fire on any receiver - a plain `Spared::status()` accessor over a `&'static str` was reported as a wait, and this tier's domain is container *status* and *health*, so the name will recur. Declared rather than hidden: the site says it cannot tell them apart, and an allowance is the escape; resolving the receiver by SPELLING would trade it for a false negative. A wait laundered through a helper in another module is invisible, so is one in a directory nobody put in scope, and so is `use std::process::Stdio as S`, or a pipe made outside `std`. `.spawn()` is unavailable **as a consequence of this rule's shape, not of the tree** - one table answers both questions, so counting it would report two waiters where the probe spawns and the runner spawns; splitting the table by role would admit it and close the `Stdio::from(file)`-then-poll hole, and that is a design change rather than a needle. Single-line string interiors are NOT blanked, so a one-line literal naming one of them is a live anchor - none is in scope today. Line-oriented, so a call split across two lines reads as none; the formatter is what owns that shape |
| A published API page carries no link destination mkdocs cannot follow, and `just validate` proves the site RENDERS | Two mechanisms over one defect, because a byte-compare cannot see it, and **both ask `urlsplit`'s question rather than a Rust-grammar one**: a scheme mkdocs does not recognise is left alone and published as a **dead href at exit 0** (`crate:`), and a destination with NO scheme (`sutura_domain:`, because `_` is not a scheme character) goes to relative-path handling and **aborts `--strict`**. `docs/.tools/rustdoc_to_markdown.py` calls `urlsplit` itself and drops such a destination, keeping the text, so no page can be *generated* with one; `check-api-links` is the rule over the output, against an ALLOWLIST of real schemes that fails closed, and it compares that list against the generator's copy. Fails closed on an unreadable page, a page it cannot lex, a directory with no generated page, a generator whose list it cannot find, and - the floor that matters - **`scanned == pages.len()`**, because a destination total is dominated by the one hand-written page and left 16 of 17 pages unscanned when `sweep` skipped them. The venue is `just docs` inside `just validate` - not a nix check, because the docs toolchain is Python and a nix sandbox cannot materialise a pixi env; allowed there because `nix run .#deny` in the same recipe already fails offline, measured | **A scheme-LESS destination is the site build's class, not the gate's.** The gate claims only the `::` spelling there, so a single-segment `](Foo)` is held by `--strict` aborting and by nothing else. **A real scheme is not judged further** - an `https://` that 404s is nobody's rule. **Scope is `docs/api/`**: a dead-scheme destination on a hand-written page elsewhere is `check-docs`' concern and held by nothing today (measured zero). Says nothing about whether rustdoc RESOLVED the link - rustdoc was clean under both spellings, so `broken_intra_doc_links` is not the signal for THIS class; #321's 15 unresolved links are the row below. **One shape the generator leaves and the gate then fails**, so `just api` is not its fix: link text containing a `]`. `Reads::Prose`, so a docs-only pull request defers the verdict to the `main` push |
| Every doc link rustdoc can resolve, resolves - and the run that writes the API pages is ARMED to say so | `[workspace.lints.rustdoc] broken_intra_doc_links = "forbid"`. A LINT, not a scan: rustdoc decides, and nothing here re-implements a resolver. **The venue is `cargo rustdoc` over LIBRARY packages - `just api` and `checks.api-docs` - and nothing else**; 15 unresolved links sat behind **exit 0 over 87 warnings** until it went in (#360). `forbid` and not `deny` because a crate root's `#![allow(...)]` beats `deny` at exit 0 with no diagnostic and is refused by `forbid` with `E0453`, measured. **What a lint cannot do is say whether it was armed**, and cargo hands a `[workspace.lints]` table only to a member whose own manifest says `[lints]`/`workspace = true` - so `check-api-docs` reads the manifests BEFORE it documents a crate: the table must set a level that stops a run, and every member must inherit it. The witness is a **pair from two DERIVATIONS** - the root manifest's own `members = [...]` list resolved to package names off disk, against the set `cargo metadata` resolved - compared as SETS and printed in the verdict beside the count that inherits. The first version counted both numbers off the ONE vector metadata produced, so `.take(2)` reported `2 of 2` and stayed green; and the composition is held by a test over `check` itself, because replacing its call to this module with a literal left all ten of that module's own tests green | **It reaches one class more than its name**, measured: the two `X is both a function and a module` ambiguities are this lint too, and the fix must be the `crate::`-qualified INLINE form (`[x](fn@crate::x)`) - the generator drops a destination carrying `::` and keeps the code span, while a bare `fn@x` survives into the page and aborts `--strict`. It reaches no further: `private_intra_doc_links` (67, several deliberate) and `redundant_explicit_links` (3) are separate lints, still warnings behind exit 0. It reads MANIFESTS, not rustdoc's invocation - what proves the lint FIRED is that run exiting non-zero. **The doctest lane is not a venue, measured twice over**: rustdoc's `--test` mode never runs the link-resolution pass (exit 0, no diagnostic, where `cargo doc` is 101), and `cargo test --doc -p xtask` is `no library targets found`. **So 30 errors at this level sit in the tree with no venue judging them** - `xtask` 26+1, `sutura-cli` 1, `sutura-serve` 2 - and `cargo doc --workspace` is red where it was green (#418). Whether a resolved link reaches a reader is the row above |
| An ordinal a record's amendment carries is the one it occupies, and a table renders as a table | `check-guidance`'s `pages` half, `xtask/src/guidance/pages.rs`, over every Markdown file in that gate's prose scope: a level-two `## Nth amendment` heading must carry the ordinal it holds in file order, and only the first may be unnumbered. **Started red**, on the record three concurrent branches had drifted into two *Fifth* amendments - the second after the *Sixth* - plus an unnumbered one mid-list, and on two others that never numbered a second amendment at all. The table half is a ratchet rather than a cleanup and saying so is the point: the tree held no run-on table the day it landed, and it exists because a rebase once dropped the blank line above a table header and four columns silently rendered as one paragraph. **Four fail-closed arms, and three of them are a pair of numbers from different places**: a page it cannot read or lex; `read` against `offered`, where `offered` is counted over the caller's whole list by a different predicate than the loop's, because counting both off one filter is how a walk narrowed to one directory passed at exit 0 with a real defect outside it - **and that pair holds a narrowed LOOP only**, since both sides are counted off the same `files` slice, so a narrowed DISCOVERY moves both and is held by nothing here (measured on `check-shipped-binaries`, whose own copy of this shape read `across 8 file(s)` at exit 0 under `chmod 000 .github/actions`); the sequence rule's own walk length against a count taken outside it, so a truncated walk is a mismatch rather than a shorter list; and zero amendment headings, which the file walk cannot produce. The composition itself is held too - `tree_problems` is a function with a fixture test, after a discarded `extend` left 871 tests green and the gate reporting `ok` over a planted defect | **Chronology, and every cross-reference.** Consecutive numbers say nothing about whether an amendment sits where it belongs, and a sentence on ANOTHER page citing *the seventh amendment* is prose nothing resolves - a renumber is still carried by hand. `##` only, so a `###` addendum is outside the sequence; a heading whose ordinal is not its first word reads as no amendment rather than a wrong one; and a table directly under a fenced block is invisible, because the lexer blanks code and the line above it then reads as blank. **The table half is a MEASURED list of block openers, not a grammar**: the first version reddened a heading, a thematic break and an admonition opener - all three of which this repository's own renderer still tables - while printing that they render as a paragraph, so the shapes it exempts were taken from the renderer and a shape nobody probed is treated as a paragraph and reported |
| A `repo` walk cannot lose a subject in silence, and a MIGRATED gate's printed count is its walk's own length | `xtask/src/repo/census.rs`. `Census`'s constructor is `pub(super)`, so `crate::repo` is the only module that can mint one - **one constructor by VISIBILITY, not by convention**, which is the difference from a growth rule keyed on *mentions*: there is no rule to satisfy, the type checker asks. The three functions that are the only doors into the tree return one, and the loop lives inside `Census::inspect`, so a gate never holds an iterator - `census.take(3)` is `error[E0599]: census::Census is not an iterator` and `Census::found` from another module is `error[E0624]: associated function found is private`, both measured. The three walkers are now ONE walk, because the swallowed `read_dir` error had three homes and a defect with three homes gets fixed in two: `read_dir`, the `DirEntry` and `file_type` each record a worded finding, split `NotFound`-versus-anything-else, so an ABSENT directory discovers nothing and an UNREACHABLE one refuses. `inspect` refuses five ways before any number exists - an unreachable subject from the walk, an empty discovery, an unreachable subject from the caller's own read, a predicate that judged NOTHING, and a declared anchor that was never judged (`must_judge`, generalising `nix_files`' `flake.nix` rule, which survives a scope predicate that stopped matching where a `== 0` floor does not). **There is deliberately no early-exit arm and that omission is the mechanism**; it is safe because `xtask/src` holds zero labelled loops and no `macro_rules!` outside a test fixture, so no `break` can hide from the grep that says the twelve walks need none. `check-warm-start` keeps the SECOND derivation of the same argument on purpose - `Discovered`'s private field, `Swept::over`'s length refusal, a verdict printing the witness's own length - so the generalisation and its reference implementation are never one edit apart. **Reproduced rather than quoted, on `110591d5` with `chmod 000 crates/sutura-domain/src/warehouse`:** `check-expect-thresholds` printed `ok - ... in 377 file(s)` clean and `ok - ... in 375 file(s)` narrowed, **exit 0 both times** - the denominator moving with the numerator - and refuses at exit 1 naming both files with the census. `check-shipped-binaries`, which is NOT migrated, gained the same refusal under `chmod 000 docs/adr` (`ok - 4 literal(s) across 12 file(s)` at exit 0 before) purely because `documented::pages` reaches the tree through `collect_files` - which is the argument for fixing the doors rather than the gates | **Two of the thirty-two are migrated** - `check-expect-thresholds` and `line-endings`, the second chosen because it prints NO count, so it holds the unreachable arm with no number to lean on. The other callers take `Census::into_listing`, which refuses an unreachable discovery and an empty one but hands back a `Vec` they can still narrow: the DISCOVERY half holds everywhere on day one, the LOOP half only where a gate migrated. That door is gated on a closed `Unmigrated` enum whose spent variants are `dead_code` - an error under `-D warnings`, measured - so the list shrinks under the compiler rather than under review, and a new gate reaching for the `Vec` is a one-line diff in that module. **`all_files()` prefers `git ls-files`, which reads the INDEX**, so on a git checkout the WALK never sees an unreadable directory and the refusal comes from the gate's own read - i.e. from a migrated gate only; the walk's own finding covers the nix-sandbox fallback and the two directory-scoped doors. `is_text_file` still answers `false` for a file it cannot open, so `max-lines` and `text-hygiene` lose it at DISCOVERY (`line-endings` reads before deciding scope, which closes it for that one gate alone). Nothing stops a gate calling `std::fs::read_dir` itself: a `disallowed-methods` entry on that path was probed and is not the mechanism. `inspected == discovered` stays a runtime `usize` equality - Rust has no dependent types - so what is type-level is only that the narrowing has nowhere to be written. And a census proves every DISCOVERED subject was inspected, never that the discovery QUESTION was right, nor that a per-subject `Judged` was judged correctly |
| A documented absence that stops being true is reported | `check-guidance`'s `absences` half, `xtask/src/guidance/absences.rs`: each entry pairs every wording of one absence with a scan of PRODUCTION code that would refute it, so a sentence that was true when it was written is held by the tree rather than by whoever next reads it. It is the only check in that gate that matches an authored WORDING inside a `.rs` doc comment - `constants` reads the same files and resolves an identifier rather than a sentence - and what makes the wider scope safe rather than an exception list is that only `///` and `//!` are read, so a rule table in Rust, being string literals, is invisible to it. Test code is excluded through `regions::scope`, including an out-of-line `#[cfg(test)] mod x;` whose marker sits in the parent file - the shape its subjects have. **Six ways red, and review found four of them**: an EMPTY table (both printed numbers derive from iterating it, so they collapse to a clean-looking zero), an entry no file states, a sighting whose globs open no file, a sighting whose walk is TRUNCATED inside a file (a per-FILE count cannot see it - the floor counts production LINES for that reason), a file in scope that cannot be READ, and a standing sighting. The CALL is held too: it is invoked from `tree_problems`, so `a_check_dropped_from_the_run_is_caught` covers it - measured, replacing it with a discard left 1023 tests green and printed a verdict byte-identical to a clean run over a planted refutation | **A registry, not a reader.** An absence nobody entered is held by nothing, and the wordings are literals, so a paraphrase escapes. The sighting is a literal authored per entry: deriving callers from an item's NAME was measured and rejected - on 2026-09-06 `.and(` occurred 23 times under `crates/`, one of them the call the claim was about. It reads the blanked code image AND `string_literals`, because `code_lines` blanks the interior of a string that SPANS LINES - a `\`-continued `format!` carrying the needle passed at exit 0 until it did, and 291 continued literals sit under `crates/*/src`. Each entry's globs are its reach, so a consumer outside them is invisible. And it holds one DIRECTION: a sentence asserting a presence, or one already false, is `CONTRADICTED`'s ratchet and not this |
| No `#[expect]` on a count-threshold lint | A threshold lint's cause is a NUMBER - a property of the surrounding function - so two branches can each move it correctly and only their merge is wrong. The message names the two honest fixes: split the function, or raise the threshold deliberately | The categorical lints are exempt on purpose: their cause is the code the attribute sits on |
| Nothing takes the artifacts a check inherits without the sweep that regenerates whatever baked a build root | `check-warm-start`, over every `.nix` file. The unit is a **taking** - one binding of crane's `cargoArtifacts` - because that is what the claim is about: 13 occurrences of the identifier in these files, **2 takings**, and two on one line still count as two. Each is attributed to what receives it (the pairing constructor, or an `import`ed module inlining the sweep itself) and an unattributed one is a refusal - an `inherit`ed taking included, which has no `=` for a binding scan to find. `preBuild` may be bound nowhere but the constructor **within the artifact flow**, which is what stops `//` - a one-level update - keeping the artifacts and dropping the sweep with nothing red. The warm-start route never calls the constructor, so its sweep is a property of `cargoWarmStart` instead: one owner for all five consumers, with the ORDER (below the export it resolves) and the NAMES (that variable against the one the warmer exports) both read, since a position is no more a mechanism than an ordering. Both counts are witnesses - private field, one constructor, refused unless handed one adjudication per taking and one inspection per consumer - so neither verdict can print a number its scan did not reach. Fails closed on `flake.nix` missing from the listing, a constructor binding no artifacts, two constructors, and an unclosed attrset | **Text, not evaluation** - the sandbox it runs in has no nix - so a taking assembled behind a `let` alias or by a function it does not follow is invisible, and so is anything unpacking a store path without naming `cargoArtifacts` at all. **The phase rule is SCOPED and derived** - files naming the constructor or the taking - because tree-wide would redden an unrelated module's legitimate `preBuild`, and a gate that reddens correct work gets disabled; `preBuild` is also the only phase read. And it reads no profile - the sweep derives its profile directory from cargo's own `root-output` record, so it names none and cannot clean the wrong one, which is the trap `cargo clean` fell into in the causality gate. **It answers where the sweep's text is INLINED, never that the text RUNS** - measured: comment out the script's own trailing invocation and the script exits 0, `just lint-workflows` is clean and `just hygiene` reports `ok - 32 gate(s)` over a tree that purges nothing. The effect is held in the OTHER venue, by four synthetic unit directories in the sweep's own test asserted over the filesystem (one of which proves the `$unitDir/output` limit rather than a behaviour), and that test reddens on exactly that mutation. **Reachability of the inline site is held by neither**: a sweep after an `exit`, or inside a shell conditional, in the warmer's exported string satisfies the line rules and the standalone run alike. What the two venues together DO cover is not a coincidence - CI classifies the sweep script and the warmer under no area, which fails open to `run_all`, so no diff runs the text half without the filesystem half; putting `nix/**` into `DOCS_ONLY` would end that. And the inline-scope rule is the STRING a `${..}` expands, not the file: file-wide was a printed pass over a tree where none of the five consumers swept, and the re-export refusal matched one quoting of `export CARGO_TARGET_DIR=` until an unquoted one walked past it |
| **Every gate the sweep registers has been EXECUTED against a tree it must refuse** | `xtask/src/main.rs`'s `every_registered_hygiene_gate_refuses_a_tree_it_cannot_attest`. It calls the fn pointer out of `TASKS` - the same call `run_hygiene` and `main` make - over a scratch root holding `repo::root`'s two markers plus an over-long file and a CRLF one, and requires the **exit code** of `Verdict::Fail` specifically: `Usage` (2) and `Inconclusive` (3) are non-zero too, and *could not measure* is not a clean bill. Nothing opts out - membership is the table, so there is no allowlist to ratchet, and the SIZE of the sweep is derived rather than written down anywhere. It has moved twice under one branch with no edit here: #375 added `check-conformance-bindings`, which entered the run on arrival, and #368 held its rule by WIDENING `check-warm-start` rather than registering, so it added nothing to count. **`TASKS` is a merge-time contention point** - two sessions grow it at once and neither branch-local run can see the other side, which is the 1000-line cap's lesson one level up. **Measured at `b4036f7`, when the sweep was 31:** 2 of 31 hygiene gates had any test driving the registered entry point's verdict, and 7 of 31 answered `ok` over a tree with none of their subjects - `unused-deps`, `check-expect-thresholds`, `check-skills` and `check-docs` gained a floor and the last two stopped saying *no skills tree* / *no site* about the repository they are registered in; `max-lines`, `line-endings` and `text-hygiene` are what the two seeded files are for, because for them absence is a legitimate pass. **The floor is a SET OF NAMES against `hygiene_gates`**, not a pair of counts: two counts taken off two spellings of one expression are two enforcers of one key, and `.take(18)` on BOTH left the first version green with 13 gates unexecuted. `hygiene_gates` is the registry's other reader, reconciled against the implementation plan's tables by `check-gate-classification` in both directions, so narrowing it to hide a narrowed loop reddens that gate instead. **And the process is claimed by an assertion rather than by a sentence**: `set_current_dir` is process-global, so the test refuses to run unless `NEXTEST` is set | **It says a gate REFUSED, never why**, and nothing about whether the gate's predicate is the question its consumer asks - one validating a Rust path where the renderer asks `urlsplit` refuses this tree and is still wrong about every shape it was written for, which is `github.com/telekom/sutura#371`'s own stated limit and why a reviewer who MUTATES is not replaced by it. **Vacuity only:** *violated invariant → red* and *inspected < discovered → red* are two further probes that issue asks for and this holds neither. **For about twenty of them what is exercised is the MISSING-INPUT arm** (`could not read pixi.toml`) rather than the rule itself - so it holds that a gate refuses a tree it cannot read, which is weaker than that its rule can be falsified. `Kind::Standalone` is outside it - those take arguments. The `NEXTEST` guard makes `cargo test -p xtask` report this one test as failed rather than corrupting its siblings, which is the honest trade and not a pass |
| Every shell body a devenv module hands to bash has been read by ShellCheck | Two mechanisms, and the split is the point. **The structure**, in CI: `check-devenv-shell` reads `devenv.nix`, every module its Nix `imports` names AND `devenv.yaml`'s own `imports:` list - devenv loads both, and reading only the first left a bare body in the second at `20 of 20 ... in 1 module(s)`, exit 0. It keys on the **attribute name** through `crate::workflows::code_lines`, so `scripts.a.exec`, `{ exec = }`, `enterTest`, `tasks.<n>.status` and `enterShell` are one case rather than five needles - and a THIRD arm keys on a multi-line `''` literal anywhere in a value's span at any `let` depth, because `pkgs.writeShellScriptBin "x" ''...''` projects to an application and not to a literal. The wrapper set is DERIVED transitively from whichever `let` binding reaches `writeShellApplication`, and a candidate is admitted only if every identifier it uses is one of its own parameters, an admitted wrapper, the builder or a small closed vocabulary - because *mentions a wrapper* admitted a binding that reached one on one branch and `writeShellScriptBin` on the other, and then the verdict NAMED it as the wrapper that held a body. **The emission**, on a developer machine: `check-devenv-linter` reads the wrapper's derivation `checkPhase` out of the store and requires `bash -n`, a store-path shellcheck and `-x`. Between them the argument set handed to `writeShellApplication` is CLOSED to `name`, `bashOptions`, `text`, `extraShellCheckFlags`, read over EVERY attrset in the call at every depth, with `//`, `overrideAttrs` and `overrideDerivation` refused outright - reading the first balanced attrset only left `{ ... } // { checkPhase = "true"; }` green with the verdict printing the closed set. Refuses on `discovered == 0`, an unreadable module, an import it cannot follow, an empty wrapper set and a wrapper name bound twice, each naming which arm answered; `inspected of discovered` is a private-field witness with one constructor, compared with `!=` | **The structural half runs in CI and the linter half never does** - `devenv.nix` is used by no workflow, so ShellCheck over these bodies is a `devenv shell` build and `just ship-check`. **`-x` IS passed now**, through `extraShellCheckFlags`, which the pinned builder interpolates into the DEFAULT phase - so the earlier claim in this row, that `-x` would mean overriding `checkPhase` and was therefore out of reach, was FALSE and is the correction to remember: a fourth element in a closed set is a deliberate widening, not a structural impossibility. It follows nothing today, since the one `source` in these bodies carries `source=/dev/null`. **A one-line body under a devenv option this does not know is unheld**, and so is a `''` literal that belongs to no assignment - a bare list element. `devenv.local.nix` is gitignored, so it is neither read nor held. Text, not evaluation: Nix scoping is not modelled, a body reaching a wrapper through a conditional is not followed, and a legitimate wrapper reaching for a nixpkgs helper is REFUSED until that name joins the vocabulary - the safe direction, and the failure names the identifier that kept it out. Nothing here reads the wrapper's own one-line `source`/`"$@"` prefix: moving that glue inside the wrapper made a class the gate used to catch invisible by construction |

## What no gate here reads

Every gate reads something - a manifest, a file, a line, prose. **None reads for a missing CALL.**
The `absences` row above reads the opposite direction - a call that REFUTES a documented absence - so
it cannot tell you that nothing calls something, only that something now does.
`sutura_domain::expression` and `sutura_sql::expression` are complete, tested, reachable from
nothing, and every gate passes over them. Same shape as a `disallowed-methods` entry that reads as
enforcement while resolving to nothing.

So when adding a mechanism, ask what would fail if it were absent, and check the answer is not
"nothing, because nobody calls it". `../query-surface/SKILL.md` and `../identity/SKILL.md` hold the
built-and-not-wired inventory, and **none of it may be cited as an invariant.**
