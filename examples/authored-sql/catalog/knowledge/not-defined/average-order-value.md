---
kind: not_defined
phrase: average order value
synonyms:
  - mean order value
---
This catalog defines a spread and a total, and no average. `orders_total` divided by a count of
orders would answer it, but nothing here certifies that division under a name, so asking for it is
declined rather than computed silently from the two metrics that do exist.
