[package]
name = "xtask"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Repo automation: the dependency-boundary, file-length and unused-dependency gates."

[dependencies]
serde_json = { workspace = true }

[lints]
workspace = true
