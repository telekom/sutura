---
title: Changelog
---

<!--
This page is `CHANGELOG.md` from the repository root, pulled in by pymdownx.snippets rather than
copied. git-cliff generates that file from the commit subjects, so it has one owner and this page
must never become a second copy of it.

A whole-file include on purpose: `CHANGELOG.md` carries no `[start:...]` / `[end:...]` section
markers today. An include naming a marker that is not there fails the build, which is the
behaviour we want if somebody adds markers and this page is not updated with them.
-->

--8<-- "CHANGELOG.md"
