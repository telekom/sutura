[package]
name = "sutura-bounded-read"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
# Not published to a registry: sutura ships as an OCI image, not as crates.
publish = false
description = "The bounded single-open catalog document read and sorted directory walk that the local, OKF and data-contract catalog adapters share - a small read two or more same-class adapters both need, with no network client and no sutura-config."

[dependencies]
rustix = { workspace = true }
thiserror = { workspace = true }

[lints]
workspace = true
